<Window x:Class="Resolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Resolver"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Background="#FF252526"
        WindowStartupLocation="CenterScreen"
        Title="Resolver (made by LuVairo)" Height="500" Width="400">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="10" >
            <TextBlock 
                Style="{StaticResource textblock_main_style}"   
                Text="Coefficient a:"
                />
            <TextBox
                Style="{StaticResource textbox_main_style}">
                <TextBox.Text>
                    <Binding Path="Equation.A" Mode="TwoWay" UpdateSourceTrigger="LostFocus">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBlock
                Style="{StaticResource textblock_main_style}"
                Text="Coefficient b:"
                />
            <TextBox
                Style="{StaticResource textbox_main_style}"
                Text="{Binding Path=Equation.B, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                />
            <TextBlock
                Style="{StaticResource textblock_main_style}"
                Text="Free member c:"
                />
            <TextBox
                Style="{StaticResource textbox_main_style}"
                Text="{Binding Path=Equation.C, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                />

            <Line Margin="0 10 0 0" X1="0" X2="390"
                Stroke="Gray" StrokeThickness="3"
                />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBox  TextWrapping="Wrap"
                Style="{StaticResource textbox_second_style}"
                Text="{Binding Path=Equation.Solution, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                />
            <StackPanel Orientation="Horizontal" Height="100">
                <Button
                    Style="{StaticResource button_main_style}"
                    Command="{Binding SolveCommand}"
                    Content="Solve" Cursor="Hand"
                    />
                <Button
                    Style="{StaticResource button_main_style}"
                    Command="{Binding RefreshCommand}"
                    Content="Refresh" Cursor="Hand"
                    />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
